//! slang FFI 绑定（P1 阶段）

/// slang FFI 包装
pub struct SlangFfi {
    // P1 阶段实现
}

impl SlangFfi {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for SlangFfi {
    fn default() -> Self {
        Self::new()
    }
}
